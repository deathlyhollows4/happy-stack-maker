# @Scout - Day 7 Session 2 Manual Bridge Discovery

## Goal

Verify the planner and generation metadata path for manual advanced-topic selection.

## Scope

Inspect whether a low-mastery or empty-mastery learner who manually selects an advanced topic receives the earliest open bridge node with the advanced target kept as preview context.

## File ownership

- Owns: read-only
- May inspect: `version2_implementation_plan.md`, `src/lib/practice-planner.server.ts`, `src/lib/practice-generation-plan.server.ts`, `src/lib/practice-recommendation-view.ts`, `tests/lib/practice-planner.test.ts`, `tests/lib/practice-generation-plan.test.ts`, `tests/lib/practice-recommendation-view.test.ts`

## Forbidden files

- All files

## Verification command

Read-only task

## Expected summary

Use the worker summary contract:
Result:
Evidence:
Changed files:
Risks:
Next:
