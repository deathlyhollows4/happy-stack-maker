# Forge - Day 7 Session 1 True Beginner Implementation

## Goal

Implement the minimal Day 7 Session 1 code or test changes required to prove empty mastery starts at true beginner level.

## Scope

Apply tightly scoped changes after inspecting generation, planner, recommendation, and test harness paths.

## File ownership

- Owns: focused Day 7 Session 1 source and test files selected after inspection, plus `version2_implementation_plan.md` and `mesh/next_session.md`.
- May inspect: `src/lib/**`, `src/routes/**practice**`, `tests/**`, `package.json`, `version2_implementation_plan.md`, `mesh/next_session.md`.

## Forbidden files

- Do not modify unrelated product flows, migrations, billing, auth, or hidden-test content boundaries.
- Do not edit user-owned unrelated changes in `AGENTS.md`, `CLAUDE.md`, or untracked lesson/reference assets.

## Verification command

Focused tests selected from the inspected harness, then `npx gitnexus detect-changes --repo . --scope staged`.

## Expected summary

Use the worker summary contract:
Result:
Evidence:
Changed files:
Risks:
Next:
